```
  To apply the fix, please rebuild your system:

   1 sudo nixos-rebuild switch --flake .

  After rebuilding, you can manually start the service immediately if you don't want to wait:
   1 sudo systemctl start vm-port-forward
```

```
nh os switch
nh home switch
```
