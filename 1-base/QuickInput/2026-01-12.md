```
  使用方法

  重新构建系统后：

  # 重建系统配置
  sudo nixos-rebuild switch

  # 重建 home-manager 配置
  home-manager switch

  # 启用并启动 WebDAV 挂载服务
  systemctl --user enable webdav-mount.service
  systemctl --user start webdav-mount.service

  # 检查端口转发服务状态
  systemctl status port-forward-5666
  systemctl status port-forward-445
  # ... 其他端口

  # 检查 WebDAV 挂载状态
  systemctl --user status webdav-mount.service

  现在 WebDAV 挂载和端口转发应该在重启后自动生效了
```

```
GIT_SSH_COMMAND="ssh -F /dev/null" git push

```

```
  🔄 后续启用 DankMaterialShell 的步骤

  当 GitHub API rate limit 恢复后（通常1小时），取消注释：
  1. flake.nix - dankMaterialShell 输入
  2. hosts/default.nix - dankMaterialShell 模块
  3. home/programs/desktop/default.nix - dms.nix 导入
  4. modules/home-manager/desktop-shell.nix - dms 配置
  5. home/tweaks/default.nix - 设置 desktopShell = "dms"

  或者，你也可以创建一个 GitHub Personal Access Token 来提高 API rate limit：
  1. 访问 https://github.com/settings/tokens
  2. 生成一个 token（只需要 public_repo 权限）
  3. 在 ~/.config/nix/nix.conf 添加：
  access-tokens = github.com=ghp_your_token_here


```
